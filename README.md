# PyLine Annotator

A local-first Python code explainer. Paste code, get a clean line-by-line breakdown with syntax highlighting. No API calls. No costs.

## Run it

```bash
python3 -m http.server 8080
# open http://localhost:8080
```

Or open `index.html` directly.
